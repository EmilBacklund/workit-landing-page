const HeaderContainer = ({ children }) => {
  return (
    <div
      id="archShape"
      className="mx-auto px-4 max-w-[1109.65px] bg-workitDarkPurple z-20"
    >
      {children}
    </div>
  );
};

export default HeaderContainer;
